# Supabase — project eygxousnaylqzxzwqwwe (same project as Cleo)
NEXT_PUBLIC_SUPABASE_URL=https://eygxousnaylqzxzwqwwe.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — strategy generation, ideation, scripts, check-in analysis
ANTHROPIC_API_KEY=

# Resend — sends the invite / set-password email from createClientAccount
# (and later /api/intake). Password RESET still goes via Supabase SMTP.
# RESEND_FROM must be a verified sender in your Resend account, e.g.
# "Rumi <hello@yourdomain.com>".
RESEND_API_KEY=
RESEND_FROM=

# App URL (update when custom domain is live)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Public /onboarding form gate. The form lives at
# /onboarding?k=<this value>. Any non-empty string works; generate one with
# `openssl rand -hex 24`. Unset means the form is disabled (fails closed).
ONBOARDING_FORM_KEY=
